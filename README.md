# doc-feedback-handlers

Backend for bem.info.

## Usage

Add `secret-config.js` with mongo credentials:

```js
module.exports = {
    mongo: 'mongodb://some-user:some-password@ds125388.mlab.com:25388/feedbacks'
};
```
